# M5.5 Hydro 旧代码瘦身计划

## 1. 基线和目标

M5.5 从 main `8f652fb`（M5.4 收口）开始。目标：新模块完全接管后，删除 main.cpp 中已被覆盖的重复 hydro 实现。

当前结构（审计结论）：

- **`BoundaryNodeVelocityComputation`**（main.cpp:2188）：M5.1 后已退化为 `CornerSolve::boundary_node_velocity` 的委托包装；唯一调用点（2555）可直接使用接口。
- **`RiemannPhases::run_iteration`**（3462）：M5.2 已 phase 化，RiemannSolver 使用新模块。
- 其余 hydro 更新回调（density/momentum/work/energy/EOS/sound speed/acceptance）为活动实现，M5.4 审计确认已解耦，保留。

必须保持不变：

- 活动回调、数值公式、守恒量、ghost 生命周期；
- G1/G3 reference、字段和容差 `1e-12`。

## 2. 固定门禁

每项 G0 → G1 → G3，通过后 push GitHub：

- **G0**：PowerShell 同进程可写 `TEMP/TMP/TMPDIR` 下 clean build + link；
- **G1**：Noh Uniform → Sod AMR → Sedov AMR，容差 `1e-12`，`param.ini` 恢复；
- **G2**：`N/A — retired since 2026-08-04`；
- **G3**：四进程 Sod AMR → Sedov AMR，比较 `reference/par4_*`，参数恢复。

## 3. 细粒度阶段

### S1：BoundaryNodeVelocityComputation 委托移除

- 删除 `BoundaryNodeVelocityComputation` 委托函数；
- 调用点（2555）改用 `CornerSolve::boundary_node_velocity` 直接调用；
- 保留 `corner_solver.h` 中若存在的声明一并清理。

### S2：残留审计与收口

- 审计 main.cpp 无其他被 M5.x 覆盖后可删除的 hydro 旧实现；
- 连续两次完整 G3 收口，创建 M5.5 focused commit 并 push。

## 4. 推进规则

静态审计 → 最小修改 → G0 → G1 → G3 → reference/参数/产物检查 → focused commit → push GitHub → 详细日志。任何门禁失败停留在当前项，不向后推进。
