# M10.2.1 门禁记录：只读配置冻结第一步（2026-08-13）

## 基线与范围

- 分支：main
- M10.2.1 基线：`783dda5`（M10.3.1a）
- 生产改动：
  - `src/init/initializer.h`：`Lagrangian_init_condition` 移除每次 p4est 回调对 `coord_type = plane`/`Scheme_type = ControlVolume` 的冗余重写（构造器已设相同默认值），消除"init 回调写配置"的坏味道；加注释说明配置只读语义。
  - `src/defines.h`：`p4est_data_t` 结构体声明处加 M10 整体注释（M10.1.1/10.3.1a/10.2.1/10.4.1 迁移方向 + POD 保持约束）。
- 范围说明：`coord_type`/`Scheme_type` 等只读配置字段的**读取点**（20+ 处横跨 amr/hydro/init/solver 回调）保持从 `p4est_data_t` 读——因回调拿的是 `p4est_data_t*`（user_pointer），直接删字段需 `P4estBridge`（M10.4.1）提供 config。本步先清理写路径并固化 `simulation_config()` 快照语义。
- 未修改：任何数值计算、配置默认值、`load_from_config` 解析逻辑。
- G2：`N/A — retired since 2026-08-04`
- reference：未修改或重新生成
- `param.ini` SHA-256：`55bccddd799b613c2a2ec7d7f31380f66ed7af09ec0365e087d30a78ac0b9a94`

## G0

- PowerShell 同进程可写 `TEMP/TMP/TMPDIR`；`make clean`、`make -j8`、链接 `bin/AMR_Solver.exe` 均 PASS；`git diff --check` PASS。

## G1：serial golden rollback

入口：`python/run_tests.py`；固定顺序：Noh Uniform → Sod AMR → Sedov AMR；容差：`1e-12`。

| 算例 | solver | compare | 状态 | 用时 |
|---|---:|---:|---|---:|
| Noh Uniform | 0 | 0 | PASS | 25.1 s |
| Sod AMR | 0 | 0 | PASS | 80.3 s |
| Sedov AMR | 0 | 0 | PASS | 69.5 s |

- `serial_golden_summary.json`：`status=PASS`，`param_restored`：`true`

## G3：四进程 MPI golden rollback

入口：`python/run_mpi_gates.py`；固定顺序：Sod AMR → Sedov AMR；四进程。

| 算例 | ranks | solver | compare | 状态 | 用时 |
|---|---:|---:|---:|---|---:|
| Sod AMR | 4 | 0 | 0 | PASS | 35.5 s |
| Sedov AMR | 4 | 0 | 0 | PASS | 30.8 s |

- `mpi_gate_summary.json`：`status=PASS`，`param_restored`：`true`

## 结论

M10.2.1（第一步）的 G0、G1、G3 全部通过。`init` 回调不再写入 `coord_type`/`Scheme_type` 配置字段（清理冗余重写），`p4est_data_t` 结构体加 M10 迁移方向注释。数值结果与参数完全不变，reference 未变化。未将 `.o`、输出目录、VTU/PVTU、`.plt`、summary JSON 或调试产物加入提交。
