# M5.4 守恒更新与状态接受抽取计划

## 1. 基线和目标

M5.4 从 main `d4df5d5`（M5.3 收口）开始。目标：分离 density、momentum、work、energy、EOS、sound speed、acceptance 更新，验证守恒量（质量、动量、总能量、体积）与单步阶段对照。

当前结构（审计结论）：

- 各物理量更新已是独立回调+包装函数，在 `advance_single_stage`（main.cpp:3526）中按序调用：
  1. `get_boundary_from_p4est`
  2. `CalculateHalfTimeVariable`
  3. `CalculateCornerRcpLcpNcp` + exchange
  4. `Get_AMR_BDY_info` + exchange
  5. `RiemannSolver`（M5.2 phase 化）
  6. `ComputeDivergence`
  7. `ComputeCoordinate`
  8. `UpdateDensity`（2705）
  9. `UpdateMomentumEquation`（2772）
  10. `ComputeWork`（2846）
  11. `UpdateEnergyEquation`（2906）
  12. `UpdateEquationOfState`（2937）
  13. `ComputeSoundSpeed`
  14. `AcceptNumericalSolution`（3298，主循环调用）
- 守恒量校验已有：`StatTotalEnergyError`、refine 内质量/能量校验、`check_state_invariants`。

必须保持不变：

- 各更新公式、阶段顺序、守恒量、ghost 生命周期；
- G1/G3 reference、字段和容差 `1e-12`。

## 2. 固定门禁

每项 G0 → G1 → G3，通过后 push GitHub：

- **G0**：PowerShell 同进程可写 `TEMP/TMP/TMPDIR` 下 clean build + link；
- **G1**：Noh Uniform → Sod AMR → Sedov AMR，容差 `1e-12`，`param.ini` 恢复；
- **G2**：`N/A — retired since 2026-08-04`；
- **G3**：四进程 Sod AMR → Sedov AMR，比较 `reference/par4_*`，参数恢复。

## 3. 细粒度阶段

### U1：更新阶段审计（不改代码）

- 逐阶段核对 density/momentum/work/energy/EOS/sound speed/acceptance 的输入、输出与同步边界；
- 确认各物理量更新互不耦合（可在未来独立测试）；
- 产出更新阶段结论文档。

### U2：守恒量验证

- 核对质量、动量、总能量、体积守恒在 G1/G3 下的保持；
- 通过 G1/G3 黄金实证。

### U3：收口

- 连续两次完整 G3 收口，创建 M5.4 focused commit 并 push。

## 4. 推进规则

静态审计 → 最小修改 → G0 → G1 → G3 → reference/参数/产物检查 → focused commit → push GitHub → 详细日志。任何门禁失败停留在当前项，不向后推进。
