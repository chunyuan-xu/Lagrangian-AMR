# M7.4 Simulation::run() 编排与 main.cpp 瘦身计划

## 1. 基线和目标

M7.4 从 main `cc25946`（M7.3 收口）开始。目标：新增高层 `Simulation::run()` 编排并让 main.cpp 转发，main.cpp 只保留启动。

当前结构（审计结论）：

- `advance_time_step`（main.cpp:4370）：完整时间循环，含 ghost 初始化、AMR 编排（AMRController）、hydro 阶段（RiemannSolver/Update*/Compute*）、IO（write_solution）、守恒监控（StatTotalEnergyError）、诊断（Diagnostics::check_state_invariants）。
- `main()`（main.cpp:4615）：MPI 初始化 → 配置 → 网格构建 → `advance_time_step` → 收尾。
- `advance_time_step` 依赖 main.cpp 大量静态回调（PreProcess/quadrant_*/Lagrangian_*/write_solution 等）。

必须保持不变：

- 时间循环逻辑、阶段顺序、AMR/hydro 编排、守恒量；
- G1/G3 reference、字段和容差 `1e-12`。

## 2. 固定门禁

每项 G0 → G1 → G3，通过后 push GitHub：

- **G0**：PowerShell 同进程可写 `TEMP/TMP/TMPDIR` 下 clean build + link；
- **G1**：Noh Uniform → Sod AMR → Sedov AMR，容差 `1e-12`，`param.ini` 恢复；
- **G2**：`N/A — retired since 2026-08-04`；
- **G3**：四进程 Sod AMR → Sedov AMR，比较 `reference/par4_*`，参数恢复。

## 3. 细粒度阶段

### S1：Simulation::run 接口抽取

- 新建 `src/simulation/simulation.h`，声明 `Simulation::run` 接口（用函数指针参数接收阶段回调，保持 header 独立）；
- `advance_time_step` 迁移为调用 `Simulation::run` 的委托（或直接由 `main` 调用）。

### S2：main.cpp 瘦身

- `main()` 的编排逻辑迁移到 `Simulation::run`，`main()` 只保留 MPI/p4est/配置/网格初始化与调用 run 的启动骨架。

### S3：收口

- 连续两次完整 G3 收口，创建 M7.4 focused commit 并 push。

## 4. 推进规则

静态审计 → 最小修改 → G0 → G1 → G3 → reference/参数/产物检查 → focused commit → push GitHub → 详细日志。任何门禁失败停留在当前项，不向后推进。
