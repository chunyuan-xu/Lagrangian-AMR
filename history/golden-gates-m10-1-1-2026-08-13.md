# M10.1.1 门禁记录：IO 句柄驱逐（2026-08-13）

## 基线与范围

- 分支：main
- M10.1.1 基线：`60d7bf4`（M9 收口 + context 刷新）
- 生产改动：
  - `src/defines.h`：`p4est_data_t` 移除 `EnergyFile`/`DistanceFile`/`ErrorFile`（3 个 ofstream 成员）及构造器中的 `.open()/.setf()/.precision()` 代码；结构体加注释标记迁移。
  - `src/io/io_callbacks.h`：新增 header-only 文件管理 `IOCallbacks::energy_error_file()`/`distance_profile_file()`（命名空间内 `static std::ofstream`，首次写入时惰性打开，保持原文件名/格式）；改两个写入点（`quadrant_write_distance_profiles_callback` 的 `DistanceFile`、`StatTotalEnergyError` 的 `EnergyFile`）走新接口；补 `<fstream>` include。
  - `ErrorFile` 只 open 无写入，随成员删除一并移除。
- 未修改：`.plt` 输出格式（16 位精度、blank 分隔）、能量守恒逻辑、距离剖面逻辑。
- G2：`N/A — retired since 2026-08-04`
- reference：未修改或重新生成
- `param.ini` SHA-256：`55bccddd799b613c2a2ec7d7f31380f66ed7af09ec0365e087d30a78ac0b9a94`

## G0

- PowerShell 同进程可写 `TEMP/TMP/TMPDIR`；`make clean`、`make -j8`、链接 `bin/AMR_Solver.exe` 均 PASS；`git diff --check` PASS。

## G1：serial golden rollback

入口：`python/run_tests.py`；固定顺序：Noh Uniform → Sod AMR → Sedov AMR；容差：`1e-12`。

| 算例 | solver | compare | 状态 | 用时 |
|---|---:|---:|---|---:|
| Noh Uniform | 0 | 0 | PASS | 24.8 s |
| Sod AMR | 0 | 0 | PASS | 80.4 s |
| Sedov AMR | 0 | 0 | PASS | 64.4 s |

- `serial_golden_summary.json`：`status=PASS`，`param_restored`：`true`
- `.plt` 诊断文件：`EnergyError.plt`（208KB）/`DistanceProfiles.plt`（553KB）正常生成，16 位精度与 blank 分隔格式不变。

## G3：四进程 MPI golden rollback

入口：`python/run_mpi_gates.py`；固定顺序：Sod AMR → Sedov AMR；四进程。

| 算例 | ranks | solver | compare | 状态 | 用时 |
|---|---:|---:|---:|---|---:|
| Sod AMR | 4 | 0 | 0 | PASS | 33.3 s |
| Sedov AMR | 4 | 0 | 0 | PASS | 28.3 s |

- `mpi_gate_summary.json`：`status=PASS`，`param_restored`：`true`

## 结论

M10.1.1 的 G0、G1、G3 全部通过。`p4est_data_t` 中的 3 个 ofstream 句柄已移出到 `IOCallbacks` 惰性文件管理，`p4est_data_t` 不再持有非 POD 流对象，MPI 下无文件句柄复制/析构风险。能量守恒与距离剖面输出行为不变，参数恢复，reference 未变化。未将 `.o`、输出目录、VTU/PVTU、`.plt`、summary JSON 或调试产物加入提交。
