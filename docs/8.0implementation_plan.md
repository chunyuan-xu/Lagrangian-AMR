# M8.0 main.cpp 极限瘦身计划：p4est 回调剥离

## 1. 基线和目标

M8 从 main `0e581e5`（M 系列 DoD 收口）开始。目标：把 main.cpp 遗留的 `quadrant_xxx_callback` 胶水函数按领域（AMR/Hydro/IO）就近剥离到独立模块，main.cpp 只保留编排。

当前审计（main.cpp 遗留 20 个 callback）：

- **AMR 相关**：`quadrant_predict_timestep_callback`、`quadrant_edge_minmod_estimate_callback`、`quadrant_cell_minmod_estimate_callback`、`quadrant_whether_allowing_coarsening_from_edge_callback`、`quadrant_update_after_balance_callback`、`quadrant_set_init_parent_edge_callback`
- **Hydro 相关**：`quadrant_compute_relaxed_info_callback`、`quadrant_relaxed_hanging_solver_callback`、`quadrant_compute_halftime_variable_callback`、`quadrant_parent_edge_matrix_callback`、`quadrant_corner_matrix_assemble_callback`、`quadrant_copy_velocity_from_lag_to_relax_callback`、`quadrant_corner_velocity_callback`、`quadrant_update_corner_coordinate_callback`、`quadrant_accept_center_solution_callback`、`quadrant_compute_corner_force_callback`、`quadrant_flux_relaxed_reset_callback`
- **IO/Diagnostics**：`quadrant_copy_variable_to_array_callback`、`quadrant_write_distance_profiles_callback`、`quadrant_total_energy_error_callback`

必须保持不变：数值公式、callback 顺序、ghost 生命周期、owner/remote 语义、G1/G3 reference。

## 2. 固定门禁（系统级铁律）

每个原子任务必须执行 **G0 + G1 + G3**，缺一不可：

- **G0**：PowerShell 同进程可写 `TEMP/TMP/TMPDIR` 下 `make clean && make -j8` 通过；
- **G1**：`python/run_tests.py` Noh/Sod/Sedov 串行黄金，容差 `< 1e-12`，`param.ini` 恢复；
- **G2**：`N/A — retired since 2026-08-04`；
- **G3**：`python/run_mpi_gates.py` 四进程 Sod/Sedov，比较 `reference/par4_*`，参数恢复。

通过后 focused commit + push GitHub。

## 3. 细粒度阶段

### 阶段 8.1：AMR 回调剥离（src/amr/amr_callbacks.h）

- **M8.1.1**：迁移 `quadrant_predict_timestep_callback`、`quadrant_edge_minmod_estimate_callback`、`quadrant_cell_minmod_estimate_callback`；
- **M8.1.2**：迁移 `quadrant_whether_allowing_coarsening_from_edge_callback`、`quadrant_update_after_balance_callback`、`quadrant_set_init_parent_edge_callback`。

### 阶段 8.2：Hydro 回调剥离（src/hydro/hydro_callbacks.h）

- **M8.2.1**：迁移 `quadrant_corner_matrix_assemble_callback`、`quadrant_corner_velocity_callback`、`quadrant_copy_velocity_from_lag_to_relax_callback`；
- **M8.2.2**：迁移 `quadrant_compute_halftime_variable_callback`、`quadrant_accept_center_solution_callback`、`quadrant_compute_corner_force_callback`、`quadrant_parent_edge_matrix_callback`、`quadrant_flux_relaxed_reset_callback`。

### 阶段 8.3：IO/Diagnostics 回调剥离（src/io/io_callbacks.h）

- **M8.3.1**：迁移 `quadrant_copy_variable_to_array_callback`、`quadrant_write_distance_profiles_callback`；
- **M8.3.2**：迁移 `quadrant_total_energy_error_callback` 及其它日志钩子。

### 阶段 8.4：main.cpp 最终闭环（M8.4.1）

- 审计删除冗余 `#include`/宏/局部结构体；
- 梳理主循环编排呈现 CFD 流程全景。

## 4. 推进规则

静态审计 → 最小剥离 → G0 → G1 → G3 → reference/参数/产物检查 → focused commit → push GitHub → 详细日志。任何门禁失败停留在当前项，不向后推进。
