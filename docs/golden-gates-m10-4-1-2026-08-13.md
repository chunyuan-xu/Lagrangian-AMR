# M10.4.1 门禁记录：P4estBridge 载体（2026-08-13）

## 基线与范围

- 分支：main
- M10.4.1 基线：`9324834`（M10.2.1）
- 生产改动：
  - `src/defines.h`：新增 `P4estBridge` 结构体（`p4est_data_t data` + `const SimulationModel::SimulationConfig* config`），作为 `p4est->user_pointer` 的薄载体；`p4est_data_t` 保持 POD。
  - `src/main.cpp`：注入点改为创建 `P4estBridge bridge`（`bridge.data = ctx`、`bridge.config = &startup_config`），`p4est_new_ext` 传 `&bridge`，`p4est_data` 取 `&bridge.data`。
  - **53 处 `user_pointer` 转换点**（12 个文件）从 `(p4est_data_t*)...->user_pointer` 改为 `&((P4estBridge*)...->user_pointer)->data`——回调体内 `p4est_data->xxx` 引用全部不变（桥接包装方案，低风险机械转换）。
  - 涉及文件：`amr/amr_callbacks.h`/`amr_criteria.h`、`core/trace.h`、`hydro/hydro_callbacks.h`/`hydro_controller.h`、`init/initializer.h`、`io/io_callbacks.h`、`solver/hydro_callbacks.h`/`hydro_phases.h`、`simulation/simulation.h`、`main.cpp`。
- 范围：按用户确认采用**桥接包装**方案——`p4est_data_t` 保留为 `P4estBridge::data` 成员（未删），回调体 100+ 处引用不变；`config` 指针供后续冻结配置逐步迁移。
- 未修改：任何数值计算、回调体内逻辑、MPI 通信时序。
- G2：`N/A — retired since 2026-08-04`
- reference：未修改或重新生成
- `param.ini` SHA-256：`55bccddd799b613c2a2ec7d7f31380f66ed7af09ec0365e087d30a78ac0b9a94`

## G0

- PowerShell 同进程可写 `TEMP/TMP/TMPDIR`；`make clean`、`make -j8`、链接 `bin/AMR_Solver.exe` 均 PASS；`git diff --check` PASS。

## G1：serial golden rollback

入口：`python/run_tests.py`；固定顺序：Noh Uniform → Sod AMR → Sedov AMR；容差：`1e-12`。

| 算例 | solver | compare | 状态 | 用时 |
|---|---:|---:|---|---:|
| Noh Uniform | 0 | 0 | PASS | 26.9 s |
| Sod AMR | 0 | 0 | PASS | 94.7 s |
| Sedov AMR | 0 | 0 | PASS | 66.5 s |

- `serial_golden_summary.json`：`status=PASS`，`param_restored`：`true`

## G3：四进程 MPI golden rollback

入口：`python/run_mpi_gates.py`；固定顺序：Sod AMR → Sedov AMR；四进程。

| 算例 | ranks | solver | compare | 状态 | 用时 |
|---|---:|---:|---:|---|---:|
| Sod AMR | 4 | 0 | 0 | PASS | 33.7 s |
| Sedov AMR | 4 | 0 | 0 | PASS | 28.9 s |

- `mpi_gate_summary.json`：`status=PASS`，`param_restored`：`true`

## 结论

M10.4.1 的 G0、G1、G3 全部通过。`p4est->user_pointer` 已改为 `P4estBridge` 薄载体（持 `p4est_data_t` 载荷 + 只读 config 指针），53 处转换点全部路由到 `bridge->data`，回调体数值逻辑不变。**M10 全部子任务（M10.1.1/3.1a/2.1/4.1）已收口**：`p4est_data_t` 已驱逐 IO 句柄（M10.1.1）、能量归约量（M10.3.1a）、清理配置写路径并冻结语义（M10.2.1）、并装入 `P4estBridge` 载体（M10.4.1），结构体加完整 M10 迁移注释。参数恢复，reference 未变化。未将 `.o`、输出目录、VTU/PVTU、`.plt`、summary JSON 或调试产物加入提交。
