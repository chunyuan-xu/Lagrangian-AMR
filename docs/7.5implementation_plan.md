# M7.5 Hydro 更新阶段模块化计划（DoD 闭合）

## 1. 基线和目标

M7.5 从 main `13bcfc2`（M7.4 收口）开始。目标：闭合 Definition of Done 第 16 节「main.cpp 不再承载具体数值算法」——将 hydro 更新阶段（`advance_single_stage` 中已独立的密度/动量/能量/EOS/声速/散度更新）迁移为 `src/solver/hydro_phases.h` 纯接口，main.cpp 保留 `advance_single_stage` 编排。

当前结构（审计结论）：

- `advance_single_stage`（main.cpp:3510）调用以下已独立的更新函数：
  - `ComputeDivergence`、`ComputeCoordinate`、`UpdateDensity`、`UpdateMomentumEquation`、`ComputeWork`、`UpdateEnergyEquation`、`UpdateEquationOfState`、`ComputeSoundSpeed`；
  - 这些函数的实现是 `p4est_iterate` 包装 + 回调（`quadrant_update_density_callback` 等）。
- 更新函数依赖 main.cpp 静态回调（`quadrant_update_*_callback`），迁移需传函数指针或回调。

必须保持不变：

- 阶段顺序、更新公式、守恒量、ghost 生命周期；
- G1/G3 reference、字段和容差 `1e-12`。

## 2. 固定门禁

每项 G0 → G1 → G3，通过后 push GitHub：

- **G0**：PowerShell 同进程可写 `TEMP/TMP/TMPDIR` 下 clean build + link；
- **G1**：Noh Uniform → Sod AMR → Sedov AMR，容差 `1e-12`，`param.ini` 恢复；
- **G2**：`N/A — retired since 2026-08-04`；
- **G3**：四进程 Sod AMR → Sedov AMR，比较 `reference/par4_*`，参数恢复。

## 3. 细粒度阶段

### H1：Hydro 更新阶段接口抽取

- 新建 `src/solver/hydro_phases.h`，声明 `HydroPhases` 命名空间；
- 将 `UpdateDensity`/`UpdateMomentumEquation`/`ComputeWork`/`UpdateEnergyEquation`/`UpdateEquationOfState`/`ComputeSoundSpeed`/`ComputeDivergence`/`ComputeCoordinate` 的 `p4est_iterate` 包装逻辑抽取为模块接口（用函数指针接收回调）；
- main.cpp 对应函数改为委托调用。

### H2：回调与守恒核对

- 核对各更新回调字段与公式逐字一致；
- 通过 G1/G3 实证守恒量不变。

### H3：收口

- 连续两次完整 G3 收口，创建 M7.5 focused commit 并 push。

## 4. 推进规则

静态审计 → 最小修改 → G0 → G1 → G3 → reference/参数/产物检查 → focused commit → push GitHub → 详细日志。任何门禁失败停留在当前项，不向后推进。
