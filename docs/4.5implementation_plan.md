# M4.5 AMR 旧代码瘦身计划

## 1. 基线和目标

M4.5 从 main `b1e5db6`（M4.4 收口）开始。目标：删除 main.cpp 中已被 AMRController/AMRPolicy/AMRTransfer 覆盖或遗留的零引用旧代码。

审计结论（零引用死代码清单，每个函数在 main.cpp 中仅出现于定义处）：

1. `GetRefineCornerCoords`（4009）
2. `GetRefineCornerVelos`（4057）
3. `Lagrangian_coarsen_init_condition`（829）
4. `get_quadrant_boundary_from_p4est`（762）
5. `quadrant_copy_cell_variable_to_array_callback`（3939）
6. `write_balance_solution`（4579）
7. `write_coarsen_solution`（4625）
8. `write_refine_solution`（4671）

必须保持不变：

- 所有活动回调、AMRController/Policy/Transfer 接口、主循环编排；
- 数值公式、守恒量、ghost 生命周期、owner/remote 语义；
- G1/G3 reference、字段和容差 `1e-12`。

## 2. 固定门禁

每项 G0 → G1 → G3，通过后 push GitHub：

- **G0**：PowerShell 同进程可写 `TEMP/TMP/TMPDIR` 下 clean build + link；
- **G1**：Noh Uniform → Sod AMR → Sedov AMR，容差 `1e-12`，`param.ini` 恢复；
- **G2**：`N/A — retired since 2026-08-04`；
- **G3**：四进程 Sod AMR → Sedov AMR，比较 `reference/par4_*`，参数恢复。

## 3. 细粒度阶段

### S1：零引用死代码删除

删除上述 8 个函数定义；删除后确认 G0 消除对应的 unused-function 警告。

### S2：引用残留审计

- 确认 8 个函数名不再出现在任何文件；
- 审计 main.cpp 无其他被 M4.x 覆盖后遗留的零引用函数。

### S3：收口

- 连续两次完整 G3 收口，创建 M4.5 focused commit 并 push。

## 4. 推进规则

静态审计 → 最小修改 → G0 → G1 → G3 → reference/参数/产物检查 → focused commit → push GitHub → 详细日志。任何门禁失败停留在当前项，不向后推进。
