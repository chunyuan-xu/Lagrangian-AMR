# M7 基础数学、构建系统与最终主程序计划

## 1. 基线和目标

M7 从 main `24e3491`（M6 收口）开始。目标：统一基础数学类型、核对构建系统一致性、审计最终主程序结构，且不改变任何数值容差或输出。

当前结构（审计结论）：

- **基础数学**：`src/core/vector_matrix.h` 已定义 `CDoubleVector`/`CDoubleMatrix`（含完整运算符与矩阵运算），被 `alg.cpp`/`main.cpp`/各模块广泛使用；无 `Vec2`/`Mat2` 定义。
- **Geometry/Physics**：`src/alg.cpp/.h` 提供 `GeometryAlg`/`PhysicalAlg` 纯函数；`src/physics/eos.h` 已抽取 EOS。
- **构建系统**：Makefile（`bin/AMR_Solver.exe`）+ p4est CMake（third_party）。

必须保持不变：

- 数值容差策略、`1e-12` 门禁、守恒量、G1/G3 reference；
- Makefile 构建流程与源文件集合。

## 2. 固定门禁

每项 G0 → G1 → G3，通过后 push GitHub：

- **G0**：PowerShell 同进程可写 `TEMP/TMP/TMPDIR` 下 clean build + link；
- **G1**：Noh Uniform → Sod AMR → Sedov AMR，容差 `1e-12`，`param.ini` 恢复；
- **G2**：`N/A — retired since 2026-08-04`；
- **G3**：四进程 Sod AMR → Sedov AMR，比较 `reference/par4_*`，参数恢复。

## 3. 细粒度阶段

### B1：数学类型兼容别名

- 在 `src/core/vector_matrix.h` 新增 `Vec2`/`Mat2` 别名（指向 `CDoubleVector`/`CDoubleMatrix`）与 `using` 兼容声明；
- 不改动现有类型定义与运算符，确保零行为变化。

### B2：构建一致性审计

- 核对 Makefile 与 p4est CMake 编译同一源文件集合与 C++ 标准（C++14）；
- 记录两套构建的一致性结论。

### B3：最终主程序审计

- 审计 `main()` 结构（初始化→时间循环→收尾）与模块分工；
- 确认无残留旧 IO/诊断。

### B4：收口

- 连续两次完整 G3 收口，创建 M7 focused commit 并 push。

## 4. 推进规则

静态审计 → 最小修改 → G0 → G1 → G3 → reference/参数/产物检查 → focused commit → push GitHub → 详细日志。任何门禁失败停留在当前项，不向后推进。
