# M9.0 main.cpp 深度瘦身：外层包装壳函数剥离（修订版）

> 本文件修订 2026-08-12，依据实测代码行号（main.cpp 当前 1378 行）重写里程碑编号与范围，修复与既有收口记录冲突、漏项、构建模型不符等问题。
> 原计划审阅发现的问题已全部修正：编号复用冲突、新建 `.cpp` 与 header-only 架构冲突、自定义门禁偏离 G0/G1/G3、漏掉 `write_solution`、百行目标不达。

## 1. 基线和目标

M9 从 main `42d0846`（M8 收口）开始。M8 已剥离全部 22 个 `quadrant_` 回调到 `AMRCallbacks`/`HydroCallbacks`/`IOCallbacks`，main.cpp 从约 4700 行减至 2455 行。**修订时 main.cpp 为 1378 行**，M9.1.1/M9.1.2/M9.2.2/M9.3.1 已收口。M9 剩余目标：剥离剩余**包装壳函数**与遗留回调，main.cpp 压至数百行，仅保留启动骨架。

**重要约束**：M9 全程维持 **header-only** 架构（与既有迁移一致），不新建 `.cpp` 编译单元，不动 Makefile。所有壳函数迁入既有或新建的 `.h` 命名空间模块。引入 `.cpp` 属于 M7.3 统一构建系统的范围，不在 M9 内。

## 2. 已收口子任务（不重复执行）

| 编号 | 内容 | 目标 | 提交 |
|---|---|---|---|
| M9.1.1 | AMR 标记壳（`set_*_tag`） | `AMRCallbacks` | `b78f319` |
| M9.1.2 | AMR 拓扑编排壳（`refresh_after_balance`/`append_refresh_snapshot`/`Get_AMR_BDY_info`） | `AMRCallbacks` | `69daf11` |
| M9.2.1 | Hydro 回调层 | 由 M8 提前完成，跳过 | —— |
| M9.2.2 | Hydro 编排壳 18 个（`predict_timestep`/`RiemannSolver`/`MatrixAssemble`/更新族等） | `HydroController` | `6eb0890` |
| M9.3.1 | 初始化壳（`Lagrangian_init_condition`/`get_boundary_from_p4est`） | `Initializer` | `e2491af` |

> 说明：`predict_timestep` 已在 M9.2.2 迁入 `hydro_controller.h`，后续任务不得重复迁移。

## 3. 固定门禁（系统级铁律）

每个原子任务 G0 + G1 + G3，缺一不可：

- **G0**：PowerShell 同进程可写 `TEMP/TMP/TMPDIR` 下 `make clean && make -j8`，链接 `bin/AMR_Solver.exe`，`git diff --check`；
- **G1**：`python/run_tests.py` Noh/Sod/Sedov 串行黄金，容差 `< 1e-12`，`param.ini` 恢复；
- **G2**：`N/A — retired since 2026-08-04`；
- **G3**：`python/run_mpi_gates.py` 四进程 Sod/Sedov，比较 `reference/par4_*`。

通过后 focused commit + push GitHub + 门禁记录文档 `docs/golden-gates-m9-<编号>-<日期>.md`。任一门禁失败停留当前项，不向后推进。

## 4. 剩余壳函数实测清单（main.cpp 1378 行）

| 函数 | 行号 | 行数 | 归属模块 |
|---|---|---|---|
| `Lagrangian_replace_quads` | 558-773 | 216 | `amr_callbacks.h` `AMRCallbacks` |
| `quadrant_corner_minmod_estimate_callback` | 774-821 | 48 | `hydro_callbacks.h` `HydroCallbacks` |
| `Gradient_estimate` | 822-874 | 53 | `hydro_controller.h` `HydroController` |
| `write_solution` | 918-1062 | 145 | `io_callbacks.h` `IOCallbacks` |
| `p4est_debug_output_vtu` | 1226-1307 | 82 | `io_callbacks.h` `IOCallbacks` |
| `write_distance_profiles` | 895-917 | 23 | `io_callbacks.h` `IOCallbacks` |
| `StatTotalEnergyError` | 342-396 | 55 | `io_callbacks.h` `IOCallbacks` |
| `StatGlobalFieldChecksum` | 397-436 | 40 | `io_callbacks.h` `IOCallbacks` |
| `debug_quadrant_copy_variable_to_array_callback` | 1186-1225 | 40 | `io_callbacks.h` `IOCallbacks` |
| `advance_single_stage` | 437-557 | 121 | `hydro_controller.h` `HydroController` |
| `advance_time_step` | 1063-1185 | 123 | `simulation/simulation.h` `Simulation` |
| `Lagrangian_refine_err_estimate` | 123-128 | 6 | `amr_callbacks.h` `AMRCallbacks` |
| `Lagrangian_coarsen_err_estimate` | 129-156 | 28 | `amr_callbacks.h` `AMRCallbacks` |
| `PreProcess` | 875-894 | 20 | `hydro_controller.h` `HydroController` |
| trace 快照三件 | 58-122 | 65 | 待定（诊断） |

> `quadrant_set_gradient_zero_estimate_callback`（831 行调用点）在 M8/M9 已处理或属微调，不在本计划新增范围；若确认残留再列入清理。
> `quadtree_static`（157-341，185 行）为遗留静态函数，需先识别用途再决定归属，不预设迁移目标。

## 5. 细粒度阶段（剩余）

### 阶段 9.1：AMR 壳与回调剥离

- **M9.1.3**：迁移 `Lagrangian_replace_quads`（216 行，p4est replace 回调，父子网格数据插值）到 `AMRCallbacks`。
  - 门禁专项：AMR 前后质量/能量守恒由 G3 逐点比对覆盖，无需自定义门禁。
- **M9.1.4**：迁移 `Lagrangian_refine_err_estimate`（6 行）、`Lagrangian_coarsen_err_estimate`（28 行）到 `AMRCallbacks`（p4est 误差估计回调，保持回调签名）。

### 阶段 9.2：Hydro 壳与回调剥离

- **M9.2.3**：迁移 `advance_single_stage`（121 行，Hydro 大流水线，M9.2.2 跳过项）到 `HydroController`；同时迁移 `Gradient_estimate`（53 行）、`PreProcess`（20 行）。
  - 调用点：`advance_time_step` 中 1 处、`Simulation::run` driver 签名；`advance_single_stage` 内 9 处 `StatGlobalFieldChecksum` 调用需同步路由到 `IOCallbacks::`。
- **M9.2.4**：迁移 `quadrant_corner_minmod_estimate_callback`（48 行，MUSCL 角梯度限制器）到 `HydroCallbacks`。
  - 说明：实测该回调为 48 行，非旧计划所述 184 行（184 行实为 `quadtree_static` 157-341）。

### 阶段 9.3：IO 与诊断剥离

- **M9.3.2**：迁移 `write_solution`（145 行，主写盘器）、`p4est_debug_output_vtu`（82 行）、`debug_quadrant_copy_variable_to_array_callback`（40 行）到 `IOCallbacks`。
  - 调用点：`advance_time_step` 中 3 处 `write_solution`；注意 `write_solution` 依赖 `debug_quadrant_copy_variable_to_array_callback`，须同批迁移。
- **M9.3.3**：迁移 `write_distance_profiles`（23 行）、`StatTotalEnergyError`（55 行）、`StatGlobalFieldChecksum`（40 行）到 `IOCallbacks`。
  - 调用点：`advance_single_stage` 9 处 `StatGlobalFieldChecksum`、`advance_time_step` 2 处 `StatTotalEnergyError`、1 处 `write_distance_profiles`，均需同步改路由。
  - 诊断属 `diagnostics` 还是 `io`：按 `9.0` 既有规划，`io_callbacks.h` 已含 `quadrant_total_energy_error_callback`，故统计函数归 `IOCallbacks` 与现状一致。

### 阶段 9.4：main.cpp 终极编排

- **M9.4.1**：迁移 `advance_time_step`（123 行）到 `Simulation`（`Simulation::run` driver）；清理剩余冗余与 trace 快照诊断；main.cpp 压至数百行。
  - 目标表述：**数百行**（非百行以内，与实际可迁移总量相符）；若用户要求百行以内，需追加误差估计器/`PreProcess`/trace 之外的边界清理，另行评估。

## 6. 推进规则

静态审计 → 最小剥离 → G0 → G1 → G3 → reference/参数/产物检查 → focused commit → push GitHub → 门禁记录文档 + 更新 `docs/context.md`（按 reconstruction.md §2.6）。任何门禁失败停留在当前项，不向后推进。

## 7. 每项收口后更新

按 reconstruction.md §2.6 规则，每完成一个子里程碑同步更新 `docs/context.md` 进度表与剩余清单。
