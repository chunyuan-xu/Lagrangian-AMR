# M9.0 main.cpp 深度瘦身：外层包装壳函数剥离

## 1. 基线和目标

M9 从 main `42d0846`（M8 收口）开始。M8 已剥离全部 22 个 `quadrant_` 回调到 `AMRCallbacks`/`HydroCallbacks`/`IOCallbacks`，main.cpp 从约 4700 行减至 2455 行。M9 目标：剥离剩余的**包装壳函数**（`set_*_tag`/`RiemannSolver`/`MatrixAssemble`/`advance_single_stage`/init/写盘/诊断等），main.cpp 压至数百行，仅保留启动骨架。

## 2. 审阅修正

- **M9.2.1（新建 hydro_callbacks 承载回调）已由 M8 完成**，本计划跳过回调层创建，直接迁移包装壳；
- `advance_single_stage` 是 Hydro 大流水线，迁入 `hydro_controller`；`advance_time_step` 保留为 `Simulation::run` 的 driver 或迁入 simulation 模块（M7.4 边界）；
- `Lagrangian_init_condition` 是 p4est 初始化回调（`p4est_new_ext` 传入），迁入 `src/init/` 需保持 p4est 签名。

## 3. 固定门禁（系统级铁律）

每个原子任务 G0 + G1 + G3，缺一不可：

- **G0**：PowerShell 同进程可写 `TEMP/TMP/TMPDIR` 下 `make clean && make -j8`；
- **G1**：`python/run_tests.py` Noh/Sod/Sedov 串行黄金，容差 `< 1e-12`，`param.ini` 恢复；
- **G2**：`N/A — retired since 2026-08-04`；
- **G3**：`python/run_mpi_gates.py` 四进程 Sod/Sedov，比较 `reference/par4_*`。

通过后 focused commit + push GitHub。

## 4. 细粒度阶段

### 阶段 9.1：AMR 壳函数剥离（src/amr/amr_callbacks.h）

- **M9.1.1**：迁移 `set_default_refining_tag`、`set_default_coarsening_tag`、`set_allowing_coarsening_tag` 到 `AMRCallbacks`；
- **M9.1.2**：迁移 `refresh_after_balance`、`append_refresh_snapshot`、`Get_AMR_BDY_info` 到 `AMRCallbacks`。

### 阶段 9.2：Hydro 壳函数剥离（src/hydro/hydro_controller.h）

- **M9.2.1**：回调层已由 M8 完成（跳过）；迁移 `RiemannSolver`、`MatrixAssemble`、`ComputeCornerNodeVelocity`、`ComputeHangingNodeVelocityUsingConstrainedConditionByMasterNodes` 到 `HydroController`；
- **M9.2.2**：迁移 `advance_single_stage`、`predict_timestep`、`UpdateDensity`/`UpdateMomentumEquation`/`ComputeWork`/`UpdateEnergyEquation`/`UpdateEquationOfState`/`ComputeSoundSpeed`/`ComputeDivergence`/`ComputeCoordinate`/`FluxRelaxedResetZero`/`ComputeCornerAndEdgeForce`/`CalculateHalfTimeVariable`/`CalculateCornerRcpLcpNcp` 到 `HydroController`。

### 阶段 9.3：初始化与 IO 诊断剥离

- **M9.3.1**：新建 `src/init/initializer.h`，迁移 `Lagrangian_init_condition`、`get_boundary_from_p4est`；
- **M9.3.2**：迁移 `p4est_debug_output_vtu`、`write_distance_profiles`、`StatTotalEnergyError`、`StatGlobalFieldChecksum`、`write_solution` 到 `src/io/io_callbacks.h`。

### 阶段 9.4：main.cpp 终极编排

- **M9.4.1**：清理剩余冗余，main.cpp 压至数百行，仅留启动骨架 + `Simulation::run` 编排。

## 5. 推进规则

静态审计 → 最小剥离 → G0 → G1 → G3 → reference/参数/产物检查 → focused commit → push GitHub → 详细日志。任何门禁失败停留在当前项，不向后推进。
